# Computer vision project
# Cityscape dataset analyzing

We aim to detect a chosen class (people) in the cityscape dataset (pictures of street views), then create a binary mask to show where it is located.